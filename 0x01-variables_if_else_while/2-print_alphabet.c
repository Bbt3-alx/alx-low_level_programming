#include <stdio.h>
#include <stdlib.h>
/**
 * main - the program start point
 * Return: The program ending
 */
int main(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)

	{
		putchar(alphabet);
	}

	putchar('\n');
	return (0);
}
