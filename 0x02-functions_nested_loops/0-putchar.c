#include <stdio.h>
#include <stdlib.h>

/**
 * main - write program that prints _putchar, followed by a new line.
 * Return: The program return 0
 */

int main(void)
{
	char *p = "_putchar";

	putchar(*p);

	return (0);
}
