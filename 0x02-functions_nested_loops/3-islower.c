#include <ctype.h>
#include "main.h"
#include <stdlib.h>
/**
 * main - check the code.

 * Return: Always return 0.
 */
int _islower(int c)
{
	if islower(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
