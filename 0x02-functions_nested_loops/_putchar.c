#include <stdio.h>
#include <string.h>
#include <unistd.h>

/**
 *_putchar - print the string '_putchar' to stdout
 *
 * Return: On success 1.
 * On error, -1 i sreturned
 */

int _putchar(char c)
{
	return (write(1, &c, 1));
}

int _islower(int c)
{
	if (_islower(c))
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
