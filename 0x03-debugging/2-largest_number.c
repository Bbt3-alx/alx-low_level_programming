#include <stdio.h>
#include "main.h"

/**
 * main - prints the largest of 3 integers
 * Return: 0
 */
int main(void)
{
	int a, b, c;

	a = 3;
	b = 5;
	c = 7;

	largest_number(a, b, c);

	return (0);
}
